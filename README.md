## @unvented/files

### Installation

```bash
npm i @unvented/files
```

### Usage

```js
import files from '@unvented/files'

for await (let file of files('path/to/directory')) {
  // do something
}
```
